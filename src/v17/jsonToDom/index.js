import buildSpecElement from "./buildSpecElement.js";
import renderSpecToDom from "./renderSpecToDom.js";
import domToSpec from "./domToSpec.js";

export {
    buildSpecElement,
    renderSpecToDom,
    domToSpec
};

export default buildSpecElement;
