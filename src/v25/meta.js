/**
 * meta — version & identity info
 */
export const meta = {
    version: "v25.0",
    description: "Pure zero-overhead DOM engine with internally versioned, multi-layer validation ecosystem"
};

export default meta;
