import domToSpec from "./domToSpec.js";
import getStartSpec from "./getStartSpec.js";

export const reverse = {
    domToSpec,
    getStartSpec
};

export {
    domToSpec,
    getStartSpec
};

export default reverse;
