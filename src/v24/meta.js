/**
 * meta — version & identity info
 */
export const meta = {
    version: "v24.0",
    description: "Pure zero-overhead DOM engine with unidirectional acyclic architecture and decoupled validation"
};

export default meta;
