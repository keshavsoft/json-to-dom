/**
 * meta — version & identity info
 */
export const meta = {
    version: "v26.0",
    description: "Pure zero-overhead DOM engine with decoupled validation and dedicated event listener engine"
};

export default meta;
